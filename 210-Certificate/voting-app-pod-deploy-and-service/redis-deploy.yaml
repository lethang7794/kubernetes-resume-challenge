apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deploy
  labels:
    app: demo-app
    name: redis-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      name: redis-pod
      app: demo-app
  template:
    metadata:
      name: redis-pod
      labels:
        name: redis-pod
        app: demo-app
    spec:
      containers:
        - name: redis
          image: redis
          ports:
            - containerPort: 6379
